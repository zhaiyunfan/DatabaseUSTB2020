# DatabaseUSTB2020

## 安装指南

- 首先配置好你的JDK和Maven，并在IDEA里引入好，IDEA自带3.6.3版本的Maven，可以直接使用

- 先安装完整的npm，如何进行npm换源此处不再赘述

```bash
npm install -g @vue/cli	# 全局安装vue
cd 到你的git拉取好的文件夹下的my-vue文件夹
npm install || vue add element	# 在项目中安装Element for Vue
```

